<view class="base" style="background-color:rgb({{RGB}})">
    <view class="bg-img">
      <image src='{{bgsrc}}' class="bg-full" />
    </view>
    <view class="ending">
      <view class="endColor">{{ending}}</view>
      <view class="gamut">{{gamut}}</view>
      <view class="cmky">{{CMYK}}</view>
      <view class="rgb">{{RGB}}</view>
    </view>
    <view class="about">
      <view class="aboutCY">{{aboutCreative}}</view>
      <view class="aboutSH">{{aboutLife}}</view>
      <view class="aboutYSJ">{{aboutArtist}}</view>
    </view>
</view>